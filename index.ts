import { StorefrontModule } from '@vue-storefront/core/lib/modules'

export const KEY = 'cross-domain-session'

export const CrossDomainSessionModule: StorefrontModule = function ({ store, router, appConfig }) {
}
